{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Allow both local windows to use core APIs.",
  "windows": ["main", "menubar"],
  "permissions": ["core:default"]
}
