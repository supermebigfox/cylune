{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Allow the local main window to use core APIs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
