# 桌面黑洞发布验收

本表是发布门禁，不是功能愿望清单。`通过` 必须附可复核证据；未在真实发布应用中执行的项目统一标为 `待验收`，不能用单元测试代替人工视觉、权限或 Finder 行为。

状态：`通过` / `失败` / `待验收`

## 自动化与构建

| ID | 检查项 | 预期 | 状态 | 证据 |
| --- | --- | --- | --- | --- |
| A01 | 前端测试与生产构建 | `npm test`、`npm run build` 全部成功 | 通过 | 11 个测试文件、71 项测试通过；Vite 生产构建转换 4,590 个模块 |
| A02 | Rust 普通测试 | `cargo test` 全部成功，只有真实文件 smoke 按设计忽略 | 通过 | 124 项通过、0 失败、1 项真实文件 smoke 按设计忽略 |
| A03 | 用户自有切片文件只读 smoke | 仅通过 `BAMBU_SMOKE_3MF` 读取；导入不改余额、待结算增加；4 个工具；成功/失败/取消/50% 均结算；重复成功幂等、反转恢复；前后长度与 SHA-256 不变；日志不含完整路径 | 通过 | `cargo test smoke_real_sliced_file_from_environment -- --ignored --nocapture`：14 层、4 工具、4 种结果通过；长度 `4662275`，SHA-256 `1f1614e6092de69de08cee99b7c45d9d59c37aead47a49e5754113c1433ee9d4` 前后相同 |
| A04 | Release 构建和签名完整性 | 当前 Mac 架构的 `.app` 与 `.dmg` 存在；`codesign --verify --deep --strict` 成功 | 通过 | arm64 `.app` 通过本机 ad-hoc 严格签名验证；DMG `5,701,453` bytes，SHA-256 `93a39d5c7a2a5dae9f321fb39011729d52f5b0006ba7bbf4856623a14470f0f6`，`hdiutil verify` 有效 |
| A05 | Bundle 元数据 | 唯一 bundle identifier；配置最低系统 10.15；无 camera/microphone usage key；无 `menubar` WebView | 通过 | `CFBundleIdentifier=com.local.bambuspools`；`LSMinimumSystemVersion=10.15`；仅一个 bundle Info.plist；两项隐私键缺失；配置仅有 `main` WebView。当前交付仅适用于 Apple Silicon macOS 11+：Mach-O 为 arm64 且 `minos 11.0`；未安装 x86_64 target，未生成 universal 包。配置中的 10.15 只留给未来 Intel 构建 |

## Finder、导入与点击

| ID | 检查项 | 操作与预期 | 状态 | 证据 |
| --- | --- | --- | --- | --- |
| M01 | 移动经过 Finder 文件 | 抓住黑洞本体，在桌面/Finder 文件上来回移动；不创建任务、不读取文件、不改变余额 | 待验收 | 需 Finder 实测 |
| M02 | 拖入一个受支持文件 | 将一个已切片 `.gcode.3mf` 拖入黑洞；出现导入反馈并创建/打开一个待处理任务，余额不变 | 待验收 | 需 release App 实测 |
| M03 | 拖入多个受支持文件 | 同次拖放多个普通文件；只导入列表中第一个受支持文件，不批量创建任务 | 待验收 | 需 release App 实测 |
| M04 | 未切片 3MF | 拖入仅含模型、不含 G-code 的 3MF；明确拒绝，不创建任务，不改变余额 | 待验收 | 需 release App 实测 |
| M05 | 损坏文件 | 拖入损坏的 `.3mf`；明确拒绝，不创建任务，不改变余额 | 待验收 | 需 release App 实测 |
| M06 | 不支持的文件 | 拖入图片、文本或目录；不导入，不创建任务，不改变余额 | 待验收 | 需 release App 实测 |
| M07 | 无待处理任务时短按 | 短按事件视界；打开/聚焦正常主窗口 | 待验收 | 需 release App 实测 |
| M08 | 有待处理任务时短按 | 短按事件视界；打开/聚焦最新待处理任务详情 | 待验收 | 需 release App 实测 |

## 模式、权限、尺寸与帧率

| ID | 检查项 | 操作与预期 | 状态 | 证据 |
| --- | --- | --- | --- | --- |
| M09 | 真实模式与授权 | macOS 12.3+ 选择真实模式并授予屏幕录制；重启后显示真实局部扭曲，状态为已授权 | 待验收 | 需真实权限实测 |
| M10 | 拒绝权限 | 首次请求时拒绝；自动进入轻量模式，原因明确，导入/结算仍可用 | 待验收 | 需真实权限实测 |
| M11 | 撤销权限 | 运行中在系统设置撤销；捕获停止、帧释放并稳定回退轻量模式，不崩溃 | 待验收 | 需真实权限实测 |
| M12 | 重启权限流程 | 系统要求重启时显示明确状态；彻底退出并重启一个副本后状态更新 | 待验收 | 需真实权限实测 |
| M13 | 主动轻量模式 | 设置为轻量模式；不请求/使用屏幕捕获，导入、点击与状态动画正常 | 待验收 | 需 release App 实测 |
| M14 | 尺寸边界与预设 | 滑杆只允许 120–360；分别验证 120、360 与 160/220/300 预设，窗口/命中区同步且不越界 | 待验收 | 需 release App 实测 |
| M15 | FPS 与隐藏暂停 | 分别选择自动、30、60；自动空闲 30/交互 60；隐藏后渲染和捕获为 0 FPS，再显示可恢复 | 待验收 | 需 Instruments/运行日志实测 |

## 显示器与系统生命周期

| ID | 检查项 | 操作与预期 | 状态 | 证据 |
| --- | --- | --- | --- | --- |
| M16 | 双屏移动 | 在两块屏幕间拖动并松开；位置保存到正确 display，捕获区域跟随目标屏幕 | 待验收 | 需双显示器实测 |
| M17 | Retina 比例 | 在不同 backing scale 屏幕间移动；drawable 与捕获像素尺寸更新，视觉清晰且命中区不偏移 | 待验收 | 需 Retina/外接屏实测 |
| M18 | 显示器断开/重连 | 黑洞位于外接屏时断开；迁移到主屏安全区；重连后继续可拖动，无重复黑洞 | 待验收 | 需外接屏实测 |
| M19 | 睡眠/唤醒 | 睡眠时捕获停止；唤醒后按当前权限/设置恢复，无死锁、崩溃或重复实例 | 待验收 | 需系统睡眠实测 |
| M20 | 全屏 Space | 在普通桌面与全屏 Space 间切换；黑洞显示层级和点击/拖放正常，不生成额外窗口 | 待验收 | 需全屏 Space 实测 |

## 视觉、降级与稳定性

| ID | 检查项 | 操作与预期 | 状态 | 证据 |
| --- | --- | --- | --- | --- |
| M21 | 状态视觉 | 导入时有吸入动画；待结算显示琥珀点；成功结算显示绿色反馈；失败/取消显示红色反馈 | 待验收 | 需人工视觉实测 |
| M22 | 渲染器故障隔离 | 模拟/触发 Metal 或 Shader 失败；回退轻量模式，随后文件仍可导入并结算 | 待验收 | 需故障注入实测 |
| M23 | 重复启动 | 连续启动同一个 release `.app` 两次；只存在一个进程、一个托盘入口和一只黑洞，主窗口可召回 | 待验收 | 需进程与 UI 实测 |
| M24 | 30 分钟稳定运行 | 真实模式运行 30 分钟并移动/拖放；无捕获自身递归画面，无持续单向内存增长，无崩溃 | 待验收 | 需 Activity Monitor/Instruments 实测 |

## 执行规则

- 人工验收只启动 `src-tauri/target/release/bundle/macos/拓竹耗材管家.app`；先退出开发版、旧版本和已挂载 DMG 中的副本。
- 不同时运行 `tauri dev` 和 release `.app`，否则会出现两个独立进程和两只黑洞，不能用于判定单实例。
- 用户自有 3MF 不得复制、改名、修改、上传或提交；测试只记录长度、哈希和解析摘要，不记录其完整路径。
- 任一人工项目失败时，将状态改为 `失败`，写明系统版本、复现步骤与证据，再回到对应任务补聚焦测试；不得直接把失败改成“通过”。
